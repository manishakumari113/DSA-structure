#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    int i=0;
    printf("Enter String: ");
    scanf("%s",str);
    printf("\nString With Alternate Character Displayed:  ");
    for(i=0;i<strlen(str);i=i+2)
    {
        printf("%c",str[i]);
    }
    return 0;
}
